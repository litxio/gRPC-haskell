
packages:
   ./
 , ./core
 , ./proto3-wire
 , ./proto3-suite

flags: -swagger -dhall -large-records

constraints:
  hastache == 0.6.1

allow-newer: text, aeson, bytestring, async, primitive, ghc-prim, base, criterion
